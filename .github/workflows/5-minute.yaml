name: 5-minute schedule

on:
  schedule:
    - cron: '* * * * *'

jobs:
  some-job:
    runs-on: ubuntu-latest
    container:
      image: pjlanger/hawtwheelz:latest
    steps:
      - name: Every time
        run: echo "Testing container usage"