name: 5-minute schedule

on:
  schedule:
    - cron: '* * * * *'

jobs:
  some-job:
    runs-on: ubuntu-latest
    container:
      image: alpine:latest
    steps:
      - name: Every time
        run: echo "Testing container usage"