name: 5-minute schedule

on:
  schedule:
    - cron: '* * * * *'

jobs:
  some-job:
    runs-on: ubuntu-latest
    container:
      image: pjlanger/hawtwheelz:latest
    steps:
      - name: List files
        run: ls
      - name: Read public CitiBike JSON feed
        run: python json_status_read.py